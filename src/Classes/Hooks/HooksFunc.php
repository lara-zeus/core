<?php

namespace LaraZeus\Core\Classes\Hooks;

class HooksFunc
{
    /*public function initHook($hook,$form)
    {
        $path        = app_path('Classes' . DIRECTORY_SEPARATOR . 'Forms' . DIRECTORY_SEPARATOR . 'Hooks' . DIRECTORY_SEPARATOR . 'Classes');
        $classes     = (new Common)->loadClasses($path);

        foreach ($classes as $class) {
            $chartClass = new $class;
            $chartClass->$hook($form);
            //dump($chartClass->$hook($form));
        }

        return true;
    }*/
}
